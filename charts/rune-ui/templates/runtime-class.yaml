{{- if .Values.runtimeClass.enabled }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: {{ .Values.runtimeClass.name | quote }}
  labels:
    {{- include "rune-ui.labels" . | nindent 4 }}
handler: {{ .Values.runtimeClass.handler | quote }}
{{- end }}
